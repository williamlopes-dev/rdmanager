<%= form_with(model: product_price, local: true) do |form| %>
  <% if product_price.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product_price.errors.count, "error") %> prohibited this product_price from being saved:</h2>

      <ul>
      <% product_price.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :product_id %>
    <%= form.collection_select(:product_id, Product.all, :id, :full_name) %>
  </div>

  <div class="field">
    <%= form.label :currency_id %>
    <%= form.collection_select(:currency_id, Currency.all, :id, :name) %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.number_field :price, id: :product_price_price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
